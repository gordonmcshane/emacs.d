(deftheme darkokai-overrides)

(let ((class '((class color) (min-colors 257)))
      (terminal-class '((class color) (min-colors 89))))

  ;; place for personal overrides
  ;; (custom-theme-set-variables 'darkokai-overrides)

  )
